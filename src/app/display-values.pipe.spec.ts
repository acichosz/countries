import { DisplayValuesPipe } from './display-values.pipe';

describe('DisplayValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
