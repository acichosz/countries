import { CurrenciesTransformPipe } from './currencies-transform.pipe';

describe('CurrenciesTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrenciesTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
